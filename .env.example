# =========================================================
# ENVIRONMENT CONFIGURATION
# =========================================================

# Solana RPC Configuration
# ---------------------------------------------------------
# PUBLIC RPC (rate limited, not recommended for production):
SOLANA_RPC_HTTP=https://api.mainnet-beta.solana.com
SOLANA_RPC_WS=wss://api.mainnet-beta.solana.com

# RECOMMENDED: Use a dedicated RPC provider for production
# Helius (recommended):
# SOLANA_RPC_HTTP=https://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY
# SOLANA_RPC_WS=wss://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY

# QuickNode:
# SOLANA_RPC_HTTP=https://your-endpoint.solana-mainnet.quiknode.pro/YOUR_KEY/
# SOLANA_RPC_WS=wss://your-endpoint.solana-mainnet.quiknode.pro/YOUR_KEY/

# Server Configuration (Railway sets PORT automatically)
# ---------------------------------------------------------
PORT=3000

# Logging Configuration
# ---------------------------------------------------------
# Levels: debug, info, warn, error
LOG_LEVEL=info
LOG_DIR=./logs

# =========================================================
# RAILWAY DEPLOYMENT NOTES
# =========================================================
# 
# 1. Add these variables in Railway dashboard:
#    - SOLANA_RPC_HTTP (required)
#    - SOLANA_RPC_WS (required)
#    - LOG_LEVEL (optional, defaults to 'info')
#
# 2. Railway automatically sets:
#    - PORT (for health check server)
#    - NODE_ENV=production
#
# 3. Recommended RPC providers:
#    - Helius: https://helius.xyz/ (has free tier)
#    - QuickNode: https://www.quicknode.com/
#    - Triton: https://triton.one/
#
# 4. Health check endpoint: GET /health
#    Status endpoint: GET /status
#    Metrics endpoint: GET /metrics (Prometheus format)
#
# =========================================================
